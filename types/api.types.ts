export enum ApiErrors {
    INVALID_METHOD,
    INVALID_REQUEST,
    INVALID_PARAMS,
    INVALID_PSEUDO,
    INVALID_EMAIL,
    INVALID_PASSWORD,
    EMAIL_ALREADY_EXISTS,
}

export enum ApiMethods {
    POST = 'POST'
}

